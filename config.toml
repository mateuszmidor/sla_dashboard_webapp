[DEFAULT]

# test ID to display the data matrix for
test_id = 3541

# latency low threshold; if above, then display connection as deteriorated
latency_deteriorated_ms = 200

# latency high threshold; if above, then display connection as failed
latency_failed_ms = 400

# time period between test results cache updates
data_update_period_seconds = 10

# time window to fetch the test results for
data_update_lookback_minutes = 5